# LifeOS v2 — зависимости
# pip install -r requirements.txt

# ── Backend ───────────────────────────
# (Mini App API доп. зависимостей не требует — только stdlib + fastapi ниже)
fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0
pydantic==2.7.1
python-dotenv==1.0.1
websockets==12.0
openai>=1.35.0

# ── Agent (Windows ПК) ────────────────
pyautogui==0.9.54
pyperclip==1.9.0
pycaw==20240210          # управление громкостью Windows
comtypes==1.4.2          # зависимость pycaw

# ── Vision / OCR (опционально) ────────
# Для action find_text (OCR поиск на экране):
# pytesseract==0.3.13
# Pillow уже идёт с pyautogui

# ── TTS (опционально) ─────────────────
# pyttsx3==2.90
